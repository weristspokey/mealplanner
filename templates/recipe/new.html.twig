{% extends 'base.html.twig' %}
{% block body %}

    <section class="content-section">
        <div class="container">
            <h1 class="text-center">Add Recipe</h1>
            <div class="col-md-5 m-auto">
            {{ form_start(new_recipe_form) }}
                    {{ form_end(new_recipe_form) }}
        </div></div>
    </section>
{% endblock %}
