{% extends 'base.html.twig' %}

{% block body %}
    <h1>Grocerylist</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grocerylist.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ grocerylist.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('grocerylist') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('grocerylist_edit', { 'id': grocerylist.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
